---
"eve": patch
---

Add a realtime speech channel plus React voice hook. The channel mints AI Gateway realtime client secrets and maps finalized speech turns into durable, principal-bound Eve turns, while `eve/react/voice` provides a browser hook for microphone UIs.
